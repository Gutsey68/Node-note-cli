#!/usr/bin/env node
/*
const note = process.argv[2]; // Get the note from the command line
const newNote = {
    content: note,
    id: Date.now()
};

console.log(newNote);
*/

import './src/commands.js';
